# Enforce the Node version range in package.json "engines" so installs fail
# fast on an unsupported Node (e.g. Node 26, where better-sqlite3 won't build)
# instead of crashing at runtime with a cryptic native-module error.
engine-strict=true
